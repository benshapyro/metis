# Auth.js JWE signing secret — generate with: openssl rand -base64 32
AUTH_SECRET=

# Shared demo password for single-password access
APP_PASSWORD=

# Vercel AI Gateway API key (required for non-Vercel deployments)
AI_GATEWAY_API_KEY=

# Postgres URL (for future DB sessions / chat history persistence)
POSTGRES_URL=

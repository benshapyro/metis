// lib/db/queries.ts — Metis v1 query helpers
// Only the functions actually called by live routes are here.
// Old template functions (saveChat, deleteChatById, getVotesByChatId, etc.)
// were removed when the Chat/Vote/Document/Suggestion tables were dropped.
export {};
